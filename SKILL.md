---
name: phone-buying-guide
description: 提问式手机选购顾问：先访谈明确现状/预算/用途，再联网核实参数（中关村在线/官网）与多渠道比价（京东/拼多多等），动态生成避坑否决项，输出面向大众的推荐或选购内容。数码选购系列第一块拼图，可扩展至耳机/平板。
triggers:
- 选手机
- 买手机
- 换机
- 手机推荐
- 手机怎么选
- phone buying
- smartphone recommendation
---

# 手机选购顾问（数码选购系列 v1：手机）

像懂行的朋友一样帮用户选手机：先问清"人和钱"，再联网查参数比价，最后给出能直接下单的建议，或面向大众的选购内容。方法论由 AI 掌握，不要求用户懂参数。

**架构原则（永不升级的设计）**：本文件只沉淀方法论、访谈框架、对比维度和信息源地图，**零机型/参数/价格数据**。所有机型事实（型号、参数、售价、系统更新年限）一律现场联网采集；有价值的行情快照沉淀到 memory/*.md 并带日期（仅作底价参照，使用前必须复核）。因此新手机发布不需要、也不应该修改本 skill。正文如出现机型名，仅是方法论抽象的案例原型，不构成参数依据。

## 铁律

1. **访谈先行**：没有访谈结论，不许直接报型号。
2. **方法论由你掌握**：用户只表达"现在用什么、想实现什么、预算多少"，参数权衡、优先级排序由你根据访谈结果自行判断。
3. **不能什么都想要**：当用户需求"全都要"时，主动帮他砍——指出每多一个维度的代价（通常预算翻倍），聚焦 Top 2 真实需求。
4. **动态一票否决**：避坑规则不写死，根据本次访谈现场生成（例：预算 2500 却要旗舰影像 → 先说明不现实；要战 4 年 → 小存储起步的直接淘汰；明确要轻薄 → 230g 以上降权；有品牌/售后/系统否决 → 尊重并过滤）。
5. **不凭记忆报参数和价格**：所有关键参数、当前售价必须联网核实；训练记忆和 memory 里的旧行情快照只能作交叉参照，不能直接当结论。查不到就直说并标注信息日期。
6. **初稿必复核**：数据和候选生成后，必须执行第五步的两层复核（旧机价值拆解 + 对抗性复核），复核不通过不输出。

## 第一步：需求访谈（提问式，分两轮）

一次问 4-5 个问题，禁止一次性甩 10 问。语气像朋友聊天，不用表格轰炸。

### 第一轮（摸底，5 问）

1. **性别与年龄段**：男生还是女生？大概哪个年龄段？
   - 用途：决定后面侧重追问什么——重量手感、小屏大屏、自拍/美颜、配色审美等。只作提问侧重和加权参考，不搞刻板印象；用户后续自己表达的需求永远优先。
2. **对手机参数的熟悉程度**：完全小白 / 略懂 / 很懂行？
   - 这题决定整个访谈的问法和最终输出的深度：
   - **小白** → 全程用体验语言："玩《王者荣耀》高画质卡不卡"而不是"天玑 8300"；输出时顺带一句话科普关键参数是什么意思。
   - **懂行** → 直接聊芯片型号、传感器规格、屏幕调光、快充协议；对比表保留完整参数，不加科普。
3. **现在用的什么手机？** 主要拿它干什么？哪里最不满意、哪里其实挺好用？
   - **"挺好用"必须挖到具体参数**：听到模糊评价（如"挺快""够用"）要追问确认规格——例："充电多久充满？""电池多大？"。用户说"挺快"可能是上百瓦快充这种硬实力，只记"满意"会漏掉关键维度，导致新机在这个维度静默倒退。
   - 升级动机藏在答案里。案例原型（方法论，非参数依据）：某旧机被当"备用机"，用户抱怨玩游戏/拍视频吃力，但其收音导入专业软件后达到千元级专业麦克风水准 → 真实缺口是性能，收音根本不是需求，别给新机堆"录音"卖点。
4. **预算**：上限多少？是硬上限还是可上下浮动？如果走以旧换新，确认用户认哪种口径——预算通常指"实际掏出来的钱"（= 新机到手价 − 旧机抵扣），不是新机标价。
5. **用途排序**：让用户从 游戏 / 拍照 / 拍视频 / 续航 / 轻办公 / 刷剧 / 信号 里挑前 2-3。

### 第二轮（收敛：从题库按需挑 4-5 题，不固定问卷）

根据第一轮的答案动态挑选最相关的题目追问：

- **打算用几年**？（决定性能冗余和存储大小）
- **生态与迁移**：现在在什么生态里（iPhone/华为/小米全家桶、手表耳机平板）？新机要不要接进去？微信双开要不要？
- **形态偏好**：直屏还是折叠屏？大屏还是小屏？对重量敏感吗？（结合第一轮的性别/手感线索针对性追问，例如对重量敏感就报出候选的具体克重）
- **渠道与成色**：全新、二手还是官翻？平时在什么渠道买（京东/拼多多/线下）？用户没主动提二手时，可以主动问一句"考虑不考虑二手/官翻，同价位能上一个档"。
- **硬性偏好或否决项**：品牌偏好、iOS/安卓、NFC/红外/双卡/存储大小，有没有"XX 直接不考虑"？
- **购买时间**：马上买，还是可以等新机发布/大促节点？
- **特别想实现的想法**：有什么独特用途？（直播、拍 vlog、给老人用、当备用工作机……）

### 访谈动态化原则

- 每一轮的问题都根据上一轮的答案现场生成，禁止照着固定问卷念。
- **性别/年龄段** → 只影响提问侧重与候选加权（手感、影像、审美），不是硬性筛选规则。
- **参数熟悉度** → 决定问法语言和输出深度（见第一轮第 2 题）。
- 用户明确说过的话 > 一切推断。
- 逃生阀：用户明显不想被问时，允许直接给初步推荐，但必须列出所用假设（例：默认打游戏为主、买全新、京东自营），并提示"回答几个问题能更准"。

## 第二步：需求收敛

- 把访谈结果整理成 3 行以内的"需求画像"，向用户复述确认。
- 明确排出优先级 Top 2-3，并指出哪些需求之间有冲突、代价是什么。

## 第三步：信息采集（信息源地图）

用 web_search / web_fetch 联网采集，多源交叉验证，单源不定论。

| 查什么 | 去哪查 |
|---|---|
| 参数/配置/上市时间 | 中关村在线产品库 zol.com.cn → 品牌官网规格页 → 百度百科（兜底） |
| 处理器性能定位 | 搜索"手机处理器天梯图 最新年份"，确认档位 |
| 实测表现 | 游戏帧率/发热/续航/扬声器：小白测评数据库、极客湾等实测评测，跑分不能替代实测 |
| 当前价格 | 京东自营 → 拼多多（百亿补贴）→ 天猫/官网，多渠道比价给价格区间，注明"以页面实时价为准"；同步查国补/以旧换新/平台券，报"到手价"而非标价 |
| 历史价格 | 什么值得买 / 慢慢买等查价格走势，识别"先涨后降"的假促销 |
| 口碑与坑点 | 知乎 / 酷安 / B站评测交叉验证，警惕营销软文；用户关心信号时，按其所在城市+运营商查当地实测口碑 |
| 新机节奏（等 or 买） | 官网发布信息 + 近期数码新闻；结合上市时间判断价格阶段（首发溢价 → 稳定期 → 换代清仓），给出"现在买 / 等大促 / 等换代"建议 |

注意事项：
- 二手机型需额外查行情价与成色分级（官翻/99 新/95 新差价）。
- 渠道风险：优先官方旗舰店/自营；百亿补贴机到手先验机（序列号查激活与保修状态）；二手警惕扩容机、卡贴机、有锁机、ID 锁和维修机，要求验机报告；线下防合约机、演示机；港版/有锁版差价大但售后无保障，必须明示。
- 报价必须带日期和渠道，跨渠道价差大时明确提示。
- 参数冲突时以官网规格页为准。
- 组件级能力（麦克风、扬声器、影像传感器、编解码协议）营销页不覆盖，要查官网规格页 + 专业拆解/单项对比评测（如收音实测）。
- memory 里的历史行情快照仅作底价参照与交叉验证，最终结论以本次实时采集为准；快照与实时价偏差大时明确提示。
- 采集优先级（尤其降级模式）：倒退检查所需维度（充电功率、重量、屏幕、系统更新年限）优先于完整参数表；这些维度缺失时，倒退检查结论必须标注"部分未验证"。

**平台适配注记**：本 skill 诞生于 OpenSquilla，文中的 web_search / web_discover / web_fetch / multi-search-engine 为该平台工具名；其他运行环境请映射到等价的联网检索与抓取能力，降级思路不变。

### 搜索故障降级链路（实测有效，按序降级，勿原地重试同一引擎）

1. web_search 换 provider，同时换查询措辞（相同查询会被重放拦截）。
2. web_discover 轻量链路。
3. **web_fetch 直抓百度搜索页**：`https://www.baidu.com/s?wd=URL编码后的关键词`，实测可用；每次只返回顶部答案约 200 字快照，需多换几组关键词拼凑完整信息。
4. **multi-search-engine skill 直连脚本**（绕过网关搜索链路，并行打多引擎）：`python "<网关安装目录>/skills/bundled/multi-search-engine/scripts/search.py" --query "关键词" --engines duckduckgo,brave --limit 10 --json`。duckduckgo/crossref 免 key，brave/tavily 分别需环境变量 BRAVE_SEARCH_API_KEY / TAVILY_API_KEY；未配 key 的引擎只记入 errors 不中断。运行前先确认脚本实际路径。

实测坑点：必应搜索页正文被抽稀基本无用；搜狗有验证码墙，别浪费时间；所有查询参数必须 URL 编码，否则部分站点直接报错。百度快照自带日期，正好用于信息时效标注。

降级模式附加规则：

- 快照属单源低置信：关键价格必须用至少两个不同查询互证后才能进结论。
- 快照价必须与"具体型号+存储版本"对齐后再采信；离谱 outlier（如标准版搜出接近 Pro 版的价位）大概率混入了 Pro 版/高配版/营销价，弃用或标存疑。
- 降级时部分复核必然做不了（如竞品漏配检查）：**禁止假装做过**，最终输出必须附"本次未完成检查清单"（例：同价位竞品未对比、渠道实时价未核实、国补状态未知）+ 用户自助核验路径（给出具体搜索词）。

## 第四步：候选与对比

- 生成 3-5 款候选，必须全部通过本次动态生成的否决项；候选集必须包含至少一款与旧机不同品牌的同价位竞品（防同品牌偏置）。旧机同品牌机型可因生态/迁移顺位优先，但必须给出"为什么不是那款竞品"的对比理由。
- 对比表维度（旧机必须在列，逐维与现役机对照）：价格区间 | 处理器 | 屏幕 | 影像 | **电池容量与充电功率分开列**（写成"5400mAh / 80W"两个数，不要合并成"电池/快充"一栏）| 重量手感 | **系统更新年限**（用户要用 N 年时这是硬指标，查厂商官方承诺，不要凭印象）| 防水等级 | 特色功能 | 与用户需求的匹配点。
- 每款一句话"为什么进候选"，砍掉的候选说明砍掉原因。

### 生产环境自动适配

真实使用时跳过对用户的进度直播（"开始查了""查到了"这类播报全部省去），静默完成采集→复核→输出，一轮给结论。是否追问、何时收敛自行判断。**此段只豁免过程播报，不豁免访谈**：访谈先行铁律不受影响（逃生阀场景除外）。输出后主动汇报三条：

1. **维度倒退清单**：任何候选相对旧机在哪个维度倒退（充电更慢/更重/屏幕更差/砍了红外耳机孔等）。倒退落在用户已明说的在意项里时必须置顶说明。
2. **旧机强项迁移表**：旧机优于候选的项，逐条标"新机继承 / 继承不了"。旧机独有且用户依赖的能力（组件级收音、生态联动）触发第五步 A 残值不等式评估。
3. **缺失检查清单**：本轮没查到的（实时价、竞品、国补状态），给自助核验路径。

### 版本与总成本细算

- 存储版本按用途定起步容量：拍视频/重度用 256 起，轻度用 128 勉强可接受；算清"加几百块升一档"值不值。
- 隐藏成本计入总预算：不送充电器、壳膜、碎屏险/延保。
- 候选一律用"到手价"对比：标价 − 国补 − 以旧换新 − 平台券。

## 第五步：深度复核与价值拆解（生成数据后反复对比）

初稿候选不是终稿。输出前必须完成两层复核，不通过就回炉。

**A. 旧机价值拆解（深入内部，防升级幻觉）**

- 把用户现役手机按能力逐项拆开评估：性能、屏幕、影像、**收音/麦克风**、扬声器、**电池容量+充电功率**、手感重量、生态、特殊用途（备用工作机、监控、车载、遥控器）。旧机的强项哪怕用户没在意，也是新机的隐性对比基线。
- 对每一项追问：新机全部继承吗？继承不了的能力，补回来最低要花多少钱？
- **残值不等式**：二手残值 vs 旧机仍可用能力的替代成本，两个数分开算。经典案例类型：某旧机整机残值很低，但某项组件级能力（如收音导入专业软件后达到千元级外设水准）替代成本远超残值 → 卖掉净亏，正确结论是留用改役。
- 旧机结论只有三种：直接淘汰（说清为什么）/ 改役留用（说清留它干什么）/ 卖旧贴新（算清账面得失）。

**B. 对抗性复核（反复对比）**

- 需求重叠检查：候选的卖点里有没有用户旧机已经能做到的？有就降权或砍掉。
- 漏配检查：同价位有没有明显更强、却被初稿漏掉的机型？再搜一轮确认。
- 抬杠检查：对每个候选自问杠精用户会怎么挑刺——是不是等大促更香、卖点经不经得起推敲、价格是不是营销价。
- **倒退检查（痛点修复≠全面升级）**：新机相对旧机逐维度过一遍，要么持平要么更强，出现倒退必须列出。典型盲区：换大电池的同时充电功率下降（电池变大了但充满时间反而变长）——续航痛点修复的隐性代价；同类盲区：屏幕分辨率/调光降级、砍耳机孔/红外、机身变重、系统更新年限变短。倒退是否可接受由用户需求决定，但必须先摆到台面。
- 任一检查不通过 → 修改候选或重新采集，直到复核通过再输出。
- 复核补搜最多 2 轮，防止无限查证；仍存疑的点如实标注"未验证"。

## 第六步：输出（面向大众，定位 C）

默认结构：

1. **3 秒结论**：Top 1-3 推荐 + 每款一句话理由。
2. **需求画像**：复述确认（防理解跑偏）。
3. **对比表**：核心参数 + 实时价格区间。
4. **最终建议**：选它为什么、放弃另两个为什么。
5. **避坑提醒**：列出本次动态生成的否决项和容易踩的坑。
6. **渠道与时机**：在哪个渠道买、现在买还是等节点。
7. **旧机处置**：卖掉贴钱、留用改役还是以旧换新——给明确建议和账面对比（残值 vs 替代成本）。
8. **数据置信声明**：标注信息日期与来源；联网降级时附"未完成检查清单"和自助核验路径。
9. **升级 vs 倒退一页账**：相比旧机，哪些维度升级、哪些持平、哪些倒退，一张小表说清——重点修了什么、隐性代价是什么。

用户要写选购文章/视频文案时，切换为内容结构：钩子 → 需求分析 → 参数怎么看的科普 → 候选对比 → 结论清单。面向小白读者/观众时，参数一律翻译成体验语言（例：5500mAh → 重度用一天半），科普深度随受众的参数熟悉度动态调整。内容合规自查：关键数据注明来源与截止日期（防内容快速过时翻车），不用"最/第一/吊打"等绝对化表述，引用的实测结论可追溯。

## 扩展

本 skill 是数码选购系列第一块拼图（v1 = 手机）。方法论骨架（访谈 → 收敛 → 动态否决 → 查证 → 对比 → 输出）可直接复用；后续扩展耳机/平板/笔记本时，新增对应的访谈题清单和信息源地图即可。